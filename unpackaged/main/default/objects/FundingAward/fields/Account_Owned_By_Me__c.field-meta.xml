<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account_Owned_By_Me__c</fullName>
    <description>Used for dynamic reporting (i.e. can be in reports shared with multiple users that would show each user only the Funding Award records they own)</description>
    <formula>Awardee.OwnerId = $User.Id</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Used for dynamic reporting (i.e. can be in reports shared with multiple users that would show each user only the Funding Award records they own)</inlineHelpText>
    <label>Account Owned By Me</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
